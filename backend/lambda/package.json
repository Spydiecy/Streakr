{
  "name": "streakr-lambda",
  "version": "1.0.0",
  "private": true,
  "description": "Streakr AWS Lambda handlers: settlement polling, pre-lock nudge, sentiment, result card rendering.",
  "type": "commonjs",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "node build.mjs",
    "package": "npm run build && node zip.mjs"
  },
  "dependencies": {
    "@somnia-chain/markets-sdk": "^0.29.0",
    "firebase-admin": "^12.6.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.6"
  }
}
